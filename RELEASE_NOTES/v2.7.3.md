---
title: 微信代码块手机端滚动补丁
---

## 更新内容

### 🐛 问题修复
- **修复 `2.7.2` 中代码块在微信手机端仍无法稳定横向滑动的问题**：复制/同步到微信前，代码块会转换为更接近 doocs/md 的 `pre/code` 结构，避免嵌套 table 或多层滚动容器在手机端失效。
- **修复开启行号时的滑动体验**：长代码行横向滑动时，行号列保持在左侧不动，只滚动右侧代码正文。

### 🧪 测试
- 更新代码块复制回归测试，覆盖微信手机端更友好的 `pre/code` 输出结构、长代码行横向滚动，以及开启行号时的固定行号列结构。
